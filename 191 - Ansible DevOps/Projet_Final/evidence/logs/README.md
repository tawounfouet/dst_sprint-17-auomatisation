# Logs d’exécution

Ce dossier reçoit les sorties réelles générées par `scripts/run_exam.sh` et `scripts/validate_runtime.sh`.

Aucun log runtime fictif n’est fourni. Le fichier `static-validation.txt` contient uniquement les vérifications exécutables sans machines distantes.
