# 191.07 — Tests et validation

## Pyramide de qualification

```text
              HTTP / application
                   ▲
             web → database
                   ▲
          services et ports
                   ▲
        playbook / inventory
                   ▲
          syntaxe / structure
```

## Tests statiques

```bash
./tests/static_checks.sh
```

## Tests runtime

```bash
./scripts/preflight.sh
./scripts/run_exam.sh
./scripts/validate_runtime.sh
```

La validation runtime produit des logs réels. Il ne faut pas remplacer ces preuves par des sorties fictives.

## Preuves à conserver

- sortie du déploiement initial ;
- sortie de la deuxième exécution ;
- test MySQL depuis `web1` ;
- `curl http://web1` ;
- `PLAY RECAP` ;
- éventuellement captures d’écran dans `evidence/screenshots/`.
