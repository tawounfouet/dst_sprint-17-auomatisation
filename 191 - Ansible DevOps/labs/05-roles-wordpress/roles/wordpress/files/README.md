# Static files

Ce répertoire est réservé aux fichiers statiques copiés directement par le rôle avec `ansible.builtin.copy`.

Le lab actuel n'en nécessite pas encore ; ce fichier conserve néanmoins la structure standard créée par `ansible-galaxy init`.
